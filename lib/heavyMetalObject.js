import { getArtworkForTrack } from './getArtwork'; 

async function heavyMetalObject( artist = 'Metallica', tracks ) {
    console.log('tracks', tracks)
        // Returnd obj with artist and track
        console.log(tracks);
}

export default heavyMetalObject;